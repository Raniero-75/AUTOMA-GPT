Ecco un semplice codice Python che prende in input due numeri dall'utente e restituisce la loro somma:

```python
# Prendiamo in input due numeri dall'utente
numero1 = float(input("Inserisci il primo numero: "))
numero2 = float(input("Inserisci il secondo numero: "))

# Calcoliamo la somma
somma = numero1 + numero2

# Stampiamo il risultato
print("La somma dei due numeri è: ", somma)
```

In questo codice, usiamo la funzione `input()` per ottenere un input dall'utente. Questa funzione restituisce una stringa, quindi dobbiamo convertirla in un numero utilizzando la funzione `float()`. Infine, calcoliamo la somma usando l'operatore `+` e stampiamo il risultato con la funzione `print()`.